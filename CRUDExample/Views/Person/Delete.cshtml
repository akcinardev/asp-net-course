@model PersonResponse

@{
	ViewBag.Title = "Delete Person";
}

@* <a href="~/persons/index" class="link-hover">Back to Person List</a> *@
<a asp-controller="Person" asp-action="Delete" class="link-hover">Back to Person List</a>
<h2>Delete Person</h2>

<div class="center-box">
	@* <form action="/persons/create" method="post"> *@
	<form asp-controller="Person" asp-action="Delete" method="post">

		<input type="hidden" asp-for="PersonID" />

		@* PersonName *@
		<div class="form-field flex">
			<div class="w-25">
				<label asp-for="PersonName" class="form-label pt">Person Name</label>
			</div>
			<div class="flex-1">
				@* <input type="text" id="PersonName" name="PersonName" class="form-input" /> *@
				<input asp-for="PersonName" class="form-input" disabled/>
				<span asp-validation-for="PersonName" class="text-red"></span>
			</div>
		</div>

		@* Email *@
		<div class="form-field flex">
			<div class="w-25">
				<label asp-for="Email" class="form-label pt">Email</label>
			</div>
			<div class="flex-1">
				@* <input type="text" id="Email" name="Email" class="form-input" /> *@
				<input asp-for="Email" type="email" class="form-input" disabled />
				<span asp-validation-for="Email" class="text-red"></span>
			</div>
		</div>

		@* Submit *@
		<div class="form-field flex">
			<div class="w-25">
			</div>
			<div class="flex-1">
				<button class="button button-red-back">Delete</button>
				<div asp-validation-summary="All" class="text-red"></div>
				@if (ViewBag.Errors != null)
				{
					<div>
						<ul>
							@foreach (string error in ViewBag.Errors){
								<li class="ml text-red">@error</li>
							}
						</ul>
					</div>
				}
			</div>
		</div>
	</form>
</div>